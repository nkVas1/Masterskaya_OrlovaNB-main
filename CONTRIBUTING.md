# Мастерская Орлова Н.Б. — Contributing Guide

Спасибо за интерес к проекту! Если вы хотите улучшить сайт, следуйте этому гайду.

## Начало разработки

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Коммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Пушьте на ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Стиль кода

- **TypeScript** — используйте строгую типизацию
- **Tailwind CSS** — не добавляйте custom CSS без причины
- **Компоненты** — функциональные компоненты с хуками
- **Именование** — CamelCase для компонентов, kebab-case для файлов

## Комиты

Используйте информативные сообщения:
- ✨ `feat: добавить новый компонент`
- 🐛 `fix: исправить баг с анимацией`
- 📝 `docs: обновить README`
- 🎨 `style: улучшить цветовую палитру`
- ♻️ `refactor: переписать логику компонента`

## Проверка перед пушем

```bash
npm run lint
npm run type-check
npm run build
```

## Вопросы?

Откройте Issue с тегом `❓ question`.
