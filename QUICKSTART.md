# Мастерская Орлова Н.Б. — Быстрый Старт

Спасибо за интерес к нашему проекту! Это руководство поможет вам быстро начать разработку.

## 🚀 Установка (2 минуты)

### Требования
- **Node.js** 18 или выше ([скачать](https://nodejs.org/))
- **Git** для версионирования

### Шаги

```bash
# 1. Перейдите в папку проекта
cd Masterskaya_OrlovaNB-main

# 2. Установите зависимости
npm install

# 3. Запустите сервер разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) — готово! 🎉

## 📝 Основные Команды

```bash
npm run dev        # Разработка с hot reload
npm run build      # Собрать для production
npm start          # Запустить production версию
npm run lint       # Проверить код
npm run type-check # Проверить TypeScript
```

## 📁 Структура Проекта

```
src/
├── app/
│   ├── page.tsx        ← Главная страница (отредактируйте контент здесь)
│   ├── layout.tsx      ← Layout приложения
│   └── globals.css     ← Глобальные стили
├── components/         ← React компоненты
│   ├── MagicCandle.tsx ← 3D сцена
│   ├── Section.tsx     ← Обертка для анимаций
│   └── Button.tsx      ← Кнопка
└── lib/               ← Утилиты
```

## 🎨 Быстрые Кастомизации

### Изменить Цвета
Отредактируйте `tailwind.config.ts`:
```typescript
colors: {
  void: "#050505",      // Фон (черный)
  gold: "#FFD700",      // Акценты (золото)
  parchment: "#FFFFF0", // Текст (слоновая кость)
}
```

### Изменить Контент
В `src/app/page.tsx`:
- `products` — каталог артефактов
- `craftProcess` — этапы создания
- Заголовки и описания

### Добавить Изображения
1. Положите файлы в `public/images/`
2. Импортируйте в `page.tsx`:
```tsx
import Image from 'next/image';
<Image src="/images/photo.jpg" alt="Описание" width={1920} height={1080} />
```

## 🔍 Проверка Перед Деплоем

Перед выгрузкой на GitHub убедитесь:

```bash
# Проверить ошибки TypeScript
npm run type-check

# Проверить ESLint
npm run lint

# Собрать проект
npm run build

# Проверить, что build успешен
npm start
```

Если всё зелено — готово к деплою! ✅

## 📤 Деплой на Vercel (5 минут)

### Шаг 1: GitHub

```bash
git remote add origin https://github.com/ВАШ_НИК/orlov-workshop.git
git branch -M main
git push -u origin main
```

### Шаг 2: Vercel

1. Откройте [vercel.com](https://vercel.com)
2. Авторизуйтесь через GitHub
3. Нажмите "Add New..." → "Project"
4. Выберите репо `orlov-workshop`
5. Нажмите **Deploy** 🎉

Готово! Ваш сайт доступен на `orlov-workshop.vercel.app`.

### Шаг 3: Домен (опционально)

1. Купите домен (например, `orlov-magic.ru`)
2. В Vercel: Settings → Domains → Add
3. Следуйте инструкциям по DNS

## 🆘 Проблемы?

### Ошибка "Port 3000 in use"
```bash
# Используйте другой порт
npm run dev -- -p 3001
```

### Three.js ошибка
```bash
# Переустановите зависимости
npm install three @react-three/fiber @react-three/drei
```

### Git push отказывает
```bash
# Проверьте remote
git remote -v

# Исправьте URL
git remote set-url origin https://github.com/ВАШ_НИК/orlov-workshop.git
```

## 📚 Больше Информации

- [DEPLOYMENT.md](./DEPLOYMENT.md) — подробная инструкция деплоя
- [README.md](./README.md) — полная документация проекта
- [Next.js Docs](https://nextjs.org/docs) — документация фреймворка
- [Tailwind CSS](https://tailwindcss.com/docs) — стили

## ⚡ Pro Tips

- ✨ Используйте `npm run dev` для разработки
- 🔥 Hot reload работает автоматически
- 🎯 TypeScript ловит ошибки еще до запуска
- 🚀 Vercel автоматически пересобирает при push

---

**Готовы начать? Запустите `npm run dev` и создавайте магию!** 🔮✨
