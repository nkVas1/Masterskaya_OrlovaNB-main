# 📝 Гайд по Редактированию Контента

Как обновлять содержимое сайта **Мастерская Орлова Н.Б.** без знания программирования.

---

## 🎯 Основные Файлы для Редактирования

### 1. Главная Страница (`src/app/page.tsx`)

Это основной файл сайта. Здесь находится вся видимая информация.

#### Редактируемые Секции:

**A) Каталог Артефактов**

Найдите массив `products`:

```typescript
const products = [
  {
    id: 1,
    title: "Зеркало «Око Леса»",        // ← Название
    price: "45 000 ₽",                  // ← Цена
    desc: "Зеркало в оправе из...",     // ← Описание
    tags: ["Защита", "Ясновидение"],    // ← Категории
  },
  // ... добавите больше товаров
];
```

**Как добавить новый товар:**

```typescript
{
  id: 4,  // Уникальный номер (должен быть больше, чем предыдущие)
  title: "Новое Зеркало",
  price: "55 000 ₽",
  desc: "Описание вашего артефакта",
  tags: ["Тег1", "Тег2"],  // Макс 2-3 тега
}
```

**B) Процесс Создания**

Найдите массив `craftProcess`:

```typescript
const craftProcess = [
  {
    title: "Поиск материала",
    desc: "Выбор дерева по интуиции...",
  },
  // ...
];
```

Здесь 4 этапа. Изменяйте текст как нужно.

**C) Заголовки и Описания**

```typescript
<h1 className="...">
  Мастерская <br /> 
  <span className="italic text-copper">Орлова Н.Б.</span>
</h1>

<p className="...">
  Зеркала, в которых живет душа. // ← Подзаголовок (измените)
</p>
```

### 2. Layout и Мета-теги (`src/app/layout.tsx`)

Здесь находятся заголовок сайта для поисковиков и соцсетей.

```typescript
export const metadata: Metadata = {
  title: "Ваш заголовок | Мастерская Орлова",  // ← SEO заголовок
  description: "Описание сайта для Google...",  // ← Meta description
  keywords: ["зеркало", "магия", "..."],       // ← Ключевые слова
};
```

**Как изменить:**
- `title` — видно в браузере и Google
- `description` — видно в результатах поиска
- `keywords` — помогают для SEO

### 3. Контактная Информация

В `page.tsx` найдите секцию контактов:

```typescript
<div className="flex items-center gap-2">
  <Mail size={16} /> master@orlov-magic.ru  // ← Email
</div>

<div className="flex items-center gap-2">
  <MapPin size={16} /> Псковская область, д. Глухово  // ← Адрес
</div>
```

Замените на реальные данные.

---

## 🎨 Изменение Дизайна

### 1. Цвета (`tailwind.config.ts`)

```typescript
colors: {
  void: "#050505",      // Фон (черный)
  mystic: "#1A1A1A",    // Элементы (графит)
  gold: "#FFD700",      // Акценты (золото) - ГЛАВНЫЙ ЦВЕТ
  copper: "#B87333",    // Медь (заголовки)
  parchment: "#FFFFF0", // Текст (слоновая кость)
  ember: "#FF4500",     // Огонь/пламя
}
```

**Как изменить:**
1. Откройте `tailwind.config.ts`
2. Замените hex-код (например, `#FFD700` на `#FF6B9D`)
3. Сохраните (`Ctrl+S`)
4. Сайт обновится автоматически (если запущен `npm run dev`)

**Где найти цвета:**
- [ColorHexa.com](https://www.colorhexa.com) — конвертер цветов
- [Coolors.co](https://coolors.co) — генератор палитр
- [Adobe Color](https://color.adobe.com)

### 2. Шрифты (`src/app/layout.tsx`)

```typescript
const cormorant = Cormorant_Garamond({  // Заголовки (элегантный)
  weight: ["300", "400", "500", "600", "700"],
});

const inter = Inter({  // Основной текст (читаемый)
  display: "swap",
});
```

**Доступные шрифты Google:**
- [fonts.google.com](https://fonts.google.com)
- Фильтр по языку: Russian
- Выбирайте шрифты, которые поддерживают кириллицу (русский)

### 3. Анимации

В компонентах (`Section.tsx`, `MagicCandle.tsx`) находятся анимации.

**Основная анимация появления:**
```typescript
initial={{ opacity: 0, y: 50 }}      // Начальное состояние
whileInView={{ opacity: 1, y: 0 }}   // Конечное состояние
transition={{ duration: 0.8 }}        // Время (секунды)
```

Увеличьте `duration` для медленных анимаций, уменьшьте для быстрых.

---

## 🖼️ Добавление Изображений

### Шаг 1: Подготовка Изображения

1. Сгенерируйте через [Flux.ai](https://flux.ai) или [Midjourney](https://midjourney.com)
2. Оптимизируйте размер (сожмите через [TinyPNG](https://tinypng.com))
3. Сохраните в `public/images/` с именем без пробелов (например, `master-portrait.jpg`)

### Шаг 2: Вставка в HTML

**Вариант A: Через CSS фон (легче)**

```tsx
<div
  className="w-full h-full bg-cover bg-center"
  style={{
    backgroundImage: "url('/images/master-portrait.jpg')",
  }}
/>
```

**Вариант B: Через Next.js Image (лучше для SEO)**

```tsx
import Image from 'next/image';

<Image 
  src="/images/master-portrait.jpg" 
  alt="Мастер Николай Орлов"
  width={1920}
  height={1080}
/>
```

### Размеры Рекомендуемые:

- **Hero Image:** 1920x1080px (16:9)
- **Продукт:** 1080x1350px (4:5, для мобильных)
- **Интерьер:** 1920x1080px (16:9)
- **Портрет:** 800x1000px (3:4)

Все файлы должны быть в JPG или WebP (сжатые).

---

## 📱 Адаптивность

Сайт автоматически адаптируется благодаря Tailwind CSS классам:

```tsx
<h1 className="text-5xl md:text-7xl lg:text-8xl">
  {/* Мобильные: 5xl, Планшеты: 7xl, Десктоп: 8xl */}
</h1>
```

Для собственных адаптивных элементов используйте:
- `sm:` — экраны > 640px
- `md:` — экраны > 768px
- `lg:` — экраны > 1024px
- `xl:` — экраны > 1280px

---

## 🔍 SEO Оптимизация

### Заголовки и Описания

В `layout.tsx`:

```typescript
title: "Магические зеркала | Мастерская Орлова Н.Б.",
description: "Эксклюзивные зеркала ручной работы от мастера Николая Орлова...",
```

**Рекомендации:**
- Title: 50-60 символов
- Description: 150-160 символов
- Включите главные ключевые слова

### Ключевые Слова

Рекомендуемые ключевые слова для `keywords[]`:

```typescript
keywords: [
  "магические зеркала",
  "зеркало с рунами",
  "эзотерические товары",
  "авторские зеркала",
  "ручная работа дерево",
  "магические артефакты",
  "Николай Орлов",
  "зеркало гадания",
  "защита амулет",
],
```

---

## 🔧 Развертывание Изменений

После изменений **всегда:**

```bash
# 1. Проверьте локально
npm run dev
# Откройте http://localhost:3000 и смотрите результаты

# 2. Если всё хорошо, создайте коммит
git add .
git commit -m "Обновлены товары в каталоге"

# 3. Загрузите на GitHub
git push

# Vercel АВТОМАТИЧЕСКИ пересоберет и обновит сайт!
```

---

## 📝 Пример: Добавляем Новый Товар

### Шаг 1: Откройте `src/app/page.tsx`

### Шаг 2: Найдите массив `products`

```typescript
const products = [
  {
    id: 1,
    title: "Зеркало «Око Леса»",
    price: "45 000 ₽",
    desc: "...",
    tags: ["Защита", "Ясновидение"],
  },
  // ... другие товары
];
```

### Шаг 3: Добавьте новый объект

```typescript
const products = [
  // ... существующие товары
  {
    id: 7,  // ← новый ID (больше чем 6)
    title: "Зеркало Новое",
    price: "50 000 ₽",
    desc: "Описание вашего зеркала здесь",
    tags: ["Магия", "Красота"],
  },
];
```

### Шаг 4: Сохраните и проверьте

```bash
# Сохраните (Ctrl+S)
# Проверьте в браузере на http://localhost:3000
# Должна появиться новая карточка в каталоге!
```

### Шаг 5: Загрузите

```bash
git add .
git commit -m "Добавлено новое зеркало в каталог"
git push
```

Через 1-2 минуты изменения появятся на сайте! 🎉

---

## 🆘 Ошибки при Редактировании

### Синтаксическая ошибка "," вместо ";"

```typescript
// ❌ НЕПРАВИЛЬНО
const products = [
  {
    id: 1
    title: "Зеркало",  // ← Отсутствует запятая
  }
]

// ✅ ПРАВИЛЬНО
const products = [
  {
    id: 1,  // ← Запятая на месте
    title: "Зеркало",
  },
]
```

### Ошибка: неправильные кавычки

```typescript
// ❌ НЕПРАВИЛЬНО (русские кавычки)
title: "Зеркало «Oko»"

// ✅ ПРАВИЛЬНО (английские кавычки)
title: "Зеркало 'Око'"
// или
title: `Зеркало "Око"`
```

### Ошибка: незакрытые скобки

```typescript
// ❌ НЕПРАВИЛЬНО
<div className="text-gold">
  <p>Текст</p>
// Пропущена </div>

// ✅ ПРАВИЛЬНО
<div className="text-gold">
  <p>Текст</p>
</div>
```

---

## 📚 Полезные Ссылки

- [VSCode Документация](https://code.visualstudio.com/docs)
- [Tailwind CSS Классы](https://tailwindcss.com/docs)
- [React Документация](https://react.dev)
- [Next.js Документация](https://nextjs.org/docs)

---

**Всё готово! Теперь вы можете редактировать контент самостоятельно.** ✨🔮
